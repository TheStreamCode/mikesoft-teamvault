# Documentation

This directory contains repository documentation that should not be shipped inside the WordPress.org plugin package.

- `developer/` contains extension and integration notes.
- `maintainer/` contains local development and release workflow notes.

name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  php-lint:
    name: PHP lint (${{ matrix.php }})
    runs-on: ubuntu-latest
    timeout-minutes: 10

    strategy:
      fail-fast: false
      matrix:
        php:
          - '8.0'
          - '8.1'
          - '8.2'
          - '8.3'
          - '8.4'

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - name: Setup PHP
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: ${{ matrix.php }}
          coverage: none

      - name: Lint PHP files
        run: php tools/lint-php.php

  unit-tests:
    name: PHPUnit (${{ matrix.php }})
    runs-on: ubuntu-latest
    timeout-minutes: 15

    strategy:
      fail-fast: false
      matrix:
        php:
          - '8.2'
          - '8.3'
          - '8.4'

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - name: Setup PHP
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: ${{ matrix.php }}
          coverage: none
          tools: composer:v2

      - name: Validate Composer metadata
        run: composer validate --strict

      - name: Install dependencies
        run: composer install --no-interaction --prefer-dist --no-progress

      - name: Run PHPUnit
        run: composer test

  dependency-audit:
    name: Composer dependency audit
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - name: Setup PHP
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: '8.4'
          coverage: none
          tools: composer:v2

      - name: Validate Composer metadata
        run: composer validate --strict

      - name: Audit locked dependencies
        run: composer audit --locked --no-interaction

  plugin-check:
    name: WordPress Plugin Check
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - name: Build runtime plugin directory
        run: |
          mkdir -p plugin-check-build/mikesoft-teamvault
          rsync -a ./ plugin-check-build/mikesoft-teamvault/ \
            --exclude='.git' \
            --exclude='.github' \
            --exclude='.gitignore' \
            --exclude='.gitattributes' \
            --exclude='.editorconfig' \
            --exclude='.env*' \
            --exclude='.phpunit.cache' \
            --exclude='.phpunit.result.cache' \
            --exclude='.wordpress-org' \
            --exclude='.worktrees' \
            --exclude='AGENTS.md' \
            --exclude='auth.json' \
            --exclude='CODE_OF_CONDUCT.md' \
            --exclude='CONTRIBUTING.md' \
            --exclude='README.md' \
            --exclude='README.*.md' \
            --exclude='SECURITY.md' \
            --exclude='composer.json' \
            --exclude='composer.lock' \
            --exclude='docs' \
            --exclude='coverage' \
            --exclude='node_modules' \
            --exclude='plugin-check-build' \
            --exclude='screenshots' \
            --exclude='tests' \
            --exclude='tools' \
            --exclude='vendor' \
            --exclude='*.zip'

      # Plugin Check is run inline instead of via wordpress/plugin-check-action@v1.
      # That action injects plugin-check as a URL plugin in .wp-env.json, which triggers an
      # upstream @wordpress/env bug on the ubuntu-24.04 runner image (Node 24.16 / libuv
      # 1.52.1): wp-env exits 0 without starting Docker, so the check reports
      # "Environment not initialized". Installing plugin-check via WP-CLI after wp-env start
      # avoids the broken URL-download path. See WordPress/plugin-check-action#579.
      - name: Set up Node
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: '24'

      - name: Test Plugin Check output parser
        run: node --test tests/plugin-check-output.test.mjs

      - name: Check JavaScript syntax
        run: |
          node --check assets/js/admin-app-core.js
          node --check assets/js/admin-app-governance.js
          node --check assets/js/admin-app.js
          node --check assets/js/admin-notice-dismiss.js
          node --check tools/parse-plugin-check-output.mjs

      - name: Configure wp-env (no URL plugins)
        run: |
          PLUGIN_DIR="$(realpath plugin-check-build/mikesoft-teamvault)"
          printf '{ "core": null, "plugins": [], "mappings": { "wp-content/plugins/mikesoft-teamvault": "%s" } }\n' "$PLUGIN_DIR" > .wp-env.json
          cat .wp-env.json
          npm -g --no-fund install @wordpress/env@11.12.0

      - name: Start wp-env
        uses: nick-fields/retry@ad984534de44a9489a53aefd81eb77f87c70dc60 # v4
        with:
          timeout_minutes: 10
          max_attempts: 3
          shell: bash
          command: wp-env start --update

      - name: Run WordPress Plugin Check
        run: |
          wp-env run cli wp cli info
          STOP_MARKER="$(node -e "process.stdout.write(require('node:crypto').randomUUID())")"
          echo "::stop-commands::$STOP_MARKER"

          wp-env run cli wp plugin install plugin-check --activate
          wp-env run cli wp plugin activate mikesoft-teamvault

          set +e
          wp-env run cli wp plugin check mikesoft-teamvault \
            --format=json --slug=mikesoft-teamvault \
            --require=./wp-content/plugins/plugin-check/cli.php \
            > "$RUNNER_TEMP/pc.json" 2> "$RUNNER_TEMP/pc.stderr"
          CHECK_EXIT=$?
          set -e

          echo "wp plugin check exit: $CHECK_EXIT"
          echo "----- Plugin Check results -----"
          sed 's/^/| /' "$RUNNER_TEMP/pc.json" || true
          if [ -s "$RUNNER_TEMP/pc.stderr" ]; then
            echo "----- Plugin Check stderr -----"
            sed 's/^/| /' "$RUNNER_TEMP/pc.stderr" || true
          fi
          echo ""
          echo "::$STOP_MARKER::"

          node tools/parse-plugin-check-output.mjs "$RUNNER_TEMP/pc.json" "$CHECK_EXIT"
